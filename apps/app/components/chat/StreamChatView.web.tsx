import React from 'react';

interface StreamChatViewProps {
  client: any;
  channel: any;
  theme: any;
  children?: React.ReactNode;
}

export function StreamChatView({ children }: StreamChatViewProps) {
  return null;
}

export const isStreamNativeSupported = false;
